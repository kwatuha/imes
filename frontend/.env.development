# Development Environment Configuration
# This file is used when running locally with `docker-compose up`

# API URL - Uses Vite proxy to avoid CORS issues
VITE_API_URL=/api

# For Socket.IO connections
VITE_SOCKET_URL=http://localhost:3000

# API URL without /api suffix (for services that add it manually)
VITE_API_BASE_URL=/api
