# Production Environment Configuration
# This file is used when building for production

# API URL - Uses relative path, nginx will proxy to backend
VITE_API_URL=/api

# For Socket.IO connections - uses relative path
VITE_SOCKET_URL=

# API URL without /api suffix (for services that add it manually)
VITE_API_BASE_URL=/api
